# Artifact routing

Durable invariant, protocol, schema, or accepted decision goes to
`docs/architecture/`. Multi-Card working notes go to `docs/plans/<feature>/`.
A single claimed Card's execution brief goes to
`docs/board-superpowers/plans/`. When temporary notes become authoritative,
promote the decision and delete the shadow copy.
